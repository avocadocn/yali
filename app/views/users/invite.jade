extends ../layouts/default

block content
  p 请输入您的企业邮箱
  form.form-inline(role="form" action="/users/dealActive" method="post")
    .form-group
      label.sr-only Email Host
      input.form-control(type="text" name="host" placeholder="email")
    | @
    .form-group
      label.sr-only Email Domain
      if domains.length === 1
        input(type="hidden" name="domain" value="#{domains[0]}")
        input.form-control(type="text" value="#{domains[0]}" disabled="true")
      if domains.length > 1
        select.form-control(name="domain")
          each domain in domains
            option(value="#{domain}")= domain
    button.btn.btn-success(type="submit") 激活
  p= message