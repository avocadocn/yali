mixin group_thumbnail(teams)
  .dl_row.box_head
    span.pull-left.head_title 我的小队(#{teams.length}个)
    a.text.pull-right.clearLinkUnderline(href="/company/home#/team_info" ng-cloak) 添加更多 > 
  .groups(ng-init="showFlag=false;")
    each team,i in teams
      if team.gid !=='0'
        .center-block.group(data-toggle="tooltip" title="#{team.name}" ng-show="#{i}<14 || showFlag" ng-cloak)
          if team.leader
            span.group_leader
          a.center-block(href="/group/page/#{team._id}")
            //-if team_class === 'leader'
            //-.img_div
            img.img.center-block.group_logo(ng-cloak src="#{team.logo}/32/32")
    if teams.length>15
      .center-block.group(ng-show="!showFlag" ng-click="showFlag=true;")
        a.center-block ...
            //- else
            //-   img.center-block.size_48(ng-cloak src="#{team.logo}")
        //- div.center-block.clearfix.group(ng-if="#{i}>=4" ng-show='show[#{j}]' class=(team.leader==true)?'leader':'member' title="#{team.name}")
        //-   a.center-block(href="/group/page/#{team._id}")
        //-     //-if team_class === 'leader'
        //-     img.center-block.group_logo(ng-cloak src="#{team.logo}/32/32")
        //-     //- else
        //-     //-   img.center-block.size_48(ng-cloak src="#{team.logo}")
        //-     //-这些if else 不知何用
