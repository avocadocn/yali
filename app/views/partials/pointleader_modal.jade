.modal-dialog
  .modal-content
    .modal-header
      button(class="close" data-dismiss="modal" aria-hidden="true") X
      h4.modal-title 编辑小组信息
    .modal-body
      .row
        .col-md-2
          label.info_label.control-label 队名
        .col-md-8
          input.dl_input.form-control.editToggle(type="text" name ="name" ng-model="team.name")
      hr
      .row
        .col-md-4
          label.info_label.control-label 指定队长
          li(ng-repeat= "user in users")
            a(ng-click="appointLeader(user._id)" data-dismiss="modal") {{user.nickname}}
    .modal-footer
       button.dl_btn(ng-click="saveGroupInfo(team._id,team.name)") 保存
       //todo
         button.dl_btn() 关闭小组