mixin group_list(gids)
  h3 显示在组件页面的该企业所有小组
  ul
    each group in groups
      li
        label(class='group_'+group._id for=group._id)
        a(href="/group/home/#{group._id}")= group.group_type