form(name="search_form")
  .dl_modal_header
    button.close.dl_close(class="close" data-dismiss="modal" aria-hidden="true")
      img(ng-src='/img/icons/icon_close.png')
    span 找对手
  .dl_modal_body.clearfix
    .dl_modal_container
        span.dl_modal_subtitle 搜索
    .dl_modal_container
      .dl_modal_input_container
        input.dl-form-control(placeholder="请输入搜索关键字,可以是公司名或者小队名" type="text" name="name" ng-model="s_value" required)
      .dl_modal_button
        button.dl_btn_right.search_btn(type='submit' ng-click="search()" ng-disabled="search_form.$invalid") 搜索
    .dl_modal_container
      .dl_md_7
        input(type="radio" name="searchRadio"  ng-model="search_type" value="team" id="searchRadio_team")
        span(for="searchRadio_team") 按小队搜索
      .dl_md_3
        input(type="radio" name="searchRadio"  ng-model="search_type" value="company" id="searchRadio_company")
        span(for="searchRadio_company") 按公司搜索
    .dl_modal_container(ng-show='!result')
      .opponent.clearfix
        span.dl_modal_subtitle 附近对手
        span.warning_words.margin_left_15(ng-show='!homecourt') (请准确填写小队主场信息，否则donler将无法为您推荐附近的小队！)
      .dl_row(ng-if='teams.length==0')
        span.margin_left_15 没有推荐的小队(￣^￣)ゞ
      .dl_row.team_select.clearfix(ng-repeat="team in teams track by $index" ng-class="{'active':selected_index==$index}" ng-click="selcet_team($index)")
        .dl_md_2
          img.size_40(ng-src="{{team.logo}}/40/40")
        .dl_md_10
          .group_row_1
            a(href="/group/page/{{team._id}}" target="_blank")
              span {{team.name}}
          .group_row
            span 主场:{{team.home_court[0].name}}
            span(ng-if="team.home_court[1]") 、{{team.home_court[1].name}}
    .dl_modal_container(ng-show='result')
      .dl_row.opponent
        span.dl_modal_subtitle 搜索结果
    .dl_modal_container(ng-if="companies")
      .dl_row.clearfix(ng-repeat="company in companies" ng-click="toggleTeam(company._id,$index)")
        .dl_modal_container.team_select
          .dl_logo_1
            img.size_40(ng-src="{{company.logo}}/40/40")
          .dl_md_10
            .row.margin_left_15
              a(href="/company/home/{{company._id}}" target="_blank")
                span {{company.name}}
            .row.margin_left_10
              .dl_md_3
                p 小队数:{{company.team.length}}
        .dl_modal_container(ng-if="show_team[$index]")
          .dl_row.clearfix.team_select(ng-repeat="team in teams track by $index" ng-class="{'active':selected_index==$index}" ng-click="selcet_team($index)")
            .dl_md_2
              img.size_40(ng-src="{{team.logo}}/40/40")
            .dl_md_8
              p
              .dl_md_name
                span {{team.name}}
    .dl_modal_container(ng-if="companies.length == 0" ng-show='result')
      .dl_row.clearfix.team_select(ng-repeat="team in teams track by $index" ng-class="{'active':selected_index==$index}" ng-click="selcet_team($index)")
        .dl_md_2
          img.size_40(ng-src="{{team.logo}}/40/40")
        .dl_md_10
          .group_row_1
            a(href="/group/page/{{team._id}}" target="_blank")
              span {{team.name}}
          .group_row(ng-show="team.home_court[0].name")
            span 主场:{{team.home_court[0].name}}
            span(ng-if="team.home_court[1]") 、{{team.home_court[1].name}}

  .dl_modal_footer
    button.dl_btn.dl_modal_btn(ng-click="provoke_select()" ng-disabled="selected_index==-1") 动TA一下