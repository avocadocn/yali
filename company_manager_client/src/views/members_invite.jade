.member_invite_page
  section.content-header
    h1
      | 邀请成员
  section.content
    .box.box-primary
      .box-body
        form.form-inline
          .form-group
            label(for="email") 输入邮箱:
            input.form-control#email(type="email" name="email" ng-model="email" required)
          button.btn.btn-default(ng-click="invite()") 发送邀请信
        hr
        p 邀请链接:
          span.text-primary {{company.inviteUrl}}
        hr
        #inviteKeyQrCode
        a#saveInviteKeyQrCode 下载邀请专用二维码